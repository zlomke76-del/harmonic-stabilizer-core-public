{
  "name": "harmonic-stabilizer-core",
  "version": "1.1.0-public",
  "private": false,
  "description": "Public-safe Harmonic Stabilizer core demo. Private billing, API-key provisioning, and telemetry surfaces removed.",
  "scripts": {
    "test:vectors": "node scripts/test-vectors.js",
    "check": "node --check api/evaluate.js && node --check api/health.js && node --check api/contact.js"
  },
  "dependencies": {}
}
